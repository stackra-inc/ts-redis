/**
 * @fileoverview Redis Connection Implementations
 *
 * Concrete connection classes that implement the {@link RedisConnection} interface.
 *
 * - {@link UpstashConnection} — Upstash HTTP REST API connection
 *
 * @module connections
 */

export { UpstashConnection } from './upstash.connection';
