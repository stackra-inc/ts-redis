/**
 * @fileoverview Redis React Hooks
 *
 * React hooks for accessing Redis services in components.
 *
 * - {@link useRedis} — Access the RedisManager instance
 * - {@link useRedisConnection} — Get a specific named connection
 *
 * @module hooks
 */

export { useRedis } from './use-redis';
export { useRedisConnection } from './use-redis-connection';
