/**
 * @fileoverview Redis Connectors
 *
 * Connection factory implementations that create {@link RedisConnection}
 * instances from configuration.
 *
 * - {@link UpstashConnector} — Creates Upstash HTTP REST connections
 *
 * @module connectors
 */

export { UpstashConnector } from './upstash.connector';
