/**
 * Vitest global test setup for @stackra/ts-redis.
 *
 * Runs before every test file. Use for:
 * - Global mocks
 * - Polyfills
 * - Test environment configuration
 * - Metadata cleanup
 *
 * @module @stackra/ts-redis/test-setup
 */

import "reflect-metadata";
