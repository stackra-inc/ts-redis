/**
 * @fileoverview TypeScript declaration for Vitest global APIs.
 *
 * This triple-slash directive makes `describe`, `it`, `expect`, `vi`,
 * `beforeEach`, `afterEach`, etc. available globally without explicit
 * imports in every test file.
 *
 * @see https://vitest.dev/guide/ide.html
 */
/// <reference types="vitest/globals" />
